apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: game-guessr-gke-gateway
  namespace: game-guessr
spec:
  gatewayClassName: gke-l7-global-external-managed
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    hostname: "game-guessr.robin-vidal.com"
    allowedRoutes:
      namespaces:
        from: Same