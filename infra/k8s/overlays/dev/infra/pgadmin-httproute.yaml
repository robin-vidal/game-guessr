apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pgadmin
spec:
  parentRefs:
  - name: game-guessr-gke-gateway-dev
    namespace: game-guessr-dev
  hostnames:
  - "pgadmin.dev.game-guessr.robin-vidal.com"
  rules:
  - backendRefs:
    - name: pgadmin-dev
      port: 80
